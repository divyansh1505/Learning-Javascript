let sum = (a, b) => a + b;

/* This arrow function is a shorter form of:

let sum = function(a, b) {
  return a + b;
};
*/

alert(sum(1, 2) ); // 3


let double = (n) => n*2;
num = +prompt("Enter a number to double it : ")
alert (double(num))